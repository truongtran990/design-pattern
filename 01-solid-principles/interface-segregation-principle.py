"""
What is interface:
    interface is the class contains only the empty methods

What is ISP:
    No code should be force to depend on methods it does not use. Split very large interface into many smaller interface.
"""
